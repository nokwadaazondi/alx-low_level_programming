#include "main.h"
/**
 * add - adds two integers and returns the result
 *
 * @x: number belong added
 *
 * @y:number belong added
 *
 * Return: Always 0.
 *
 */
int add(int x, int y)
{

	return (x + y);
}
